<p style="color: green"><%= notice %></p>
<div class="show-item">
  <%= render @book %>
</div>
<nav class="page-nav">
  <%= link_to t('normal.edit', name: Book.model_name.human), edit_book_path(@book) %> |
  <%= link_to t('normal.back', name: Book.model_name.human), books_path %>
  <%= button_to t('normal.delete', name: Book.model_name.human), @book, method: :delete %>
</nav>
