 <%= form_with model: [commentable, comment] do |f| %>
 
   <div class="field">
     <%= f.label :content %>
     <%= f.text_area :content, required: true %>
   </div>

   <div class="actions">
     <%= f.submit %>
   </div>

 <% end %>
